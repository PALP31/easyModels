% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obtener_posthoc.R
\name{obtener_posthoc}
\alias{obtener_posthoc}
\title{Obtener comparaciones multiples post-hoc con ajuste de Tukey}
\usage{
obtener_posthoc(
  modelo,
  predictor,
  ajuste = "tukey",
  tipo_respuesta = "response",
  infer = c(TRUE, TRUE)
)
}
\arguments{
\item{modelo}{Un objeto de modelo ajustado compatible con el paquete \code{emmeans}.}

\item{predictor}{Una cadena de caracteres que representa el nombre de la variable predictora (factor) sobre la cual se desean calcular las medias marginales estimadas.}

\item{ajuste}{Metodo de ajuste de comparaciones multiples. Por defecto,
\code{"tukey"}.}

\item{tipo_respuesta}{Escala del resultado. Use \code{"response"} para
probabilidades, tasas, medias u odds ratios en GLM/GLMM; use \code{"link"}
para la escala lineal.}

\item{infer}{Valor logico de largo 2 para intervalos y pruebas en
\code{emmeans::summary()}.}
}
\value{
Un \code{data.frame} limpio que contiene las comparaciones por pares, estimaciones, errores estandar, grados de libertad (si aplica), estadisticos de prueba y p-valores ajustados por Tukey.
}
\description{
Esta funcion realiza comparaciones multiples de medias post-hoc a partir de
modelos compatibles con \code{emmeans}: \code{lm}, \code{aov}, \code{glm},
\code{lmer}, \code{glmer} y \code{glmer.nb}.
Valida que el predictor exista en el modelo y devuelve una tabla limpia con los contrastes pareados
y el ajuste de p-valores por el metodo de Tukey.
}
\examples{
\dontrun{
  datos <- data.frame(
    y = rnorm(100),
    grupo = factor(rep(c("A", "B", "C", "D"), each = 25))
  )
  modelo <- lm(y ~ grupo, data = datos)
  obtener_posthoc(modelo, "grupo")
}
}
